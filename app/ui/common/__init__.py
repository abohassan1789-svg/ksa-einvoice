"""Shared UI building blocks for the CRM review screens.

Holds theme constants, the small style helpers, and pure data helpers that
are reused across every screen so each screen file only contains its own
layout and interactions.
"""
